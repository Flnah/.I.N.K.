const righturl = (window.location.href);
    if (window.location.href.includes("/block_screen.html?id=")){
        if (window.location.href.includes("https://localhost:")){
            const righturl = (true)
        }    
    } else {
        const righturl = (false)};

        //Delete Alerts In Future Development
        if (righturl) { 
            localStorage.setItem('Inked','f')
            alert("TEST SUCCESSFUL")
            console.log("Starting")} else {alert("TEST SUCCESSFUL")
            console.log("Wrong Site")};
        if (righturl){
            if (confirm('ᴉɟᴉM ǝlqɐsᴉp o┴ ʇuɐM no⅄ ǝɹnS no⅄ ǝɹ∀')){
            //Wifi Disabling Code
            }}

        